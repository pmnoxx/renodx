#include "ui_settings.hpp"

// Function to add UI settings to an existing settings vector
// Note: This function is now empty as all UI sections have been migrated to dedicated components
void AddUISettings(renodx::utils::settings::Settings& settings) {
    // All UI sections have been migrated to dedicated components in the ui/ directory
    // This function is kept for compatibility but is no longer used
}
